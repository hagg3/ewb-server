# --- Local-only, never published ---
CLAUDE.md
WORKING/
# Third-party reference drops (upstream/community source shared for study).
# Not ours to redistribute, and they carry operator IPs — read them, never commit them.
latestREF/
LatestREF/

# --- Operator shell scripts (ssh admin helpers; local-only for now) ---
/scripts/

# --- Compiled server + offline test binaries (rebuilt by build_server.sh) ---
/edenserver
/edenmatch
/snapz_codec_test
/region_test
/out_queue_test
/protocol_test
/matchmaker_test
/control_test
/worldedit_test
/eden_names_test
/eden_file_test
/eden_import
/eden_import_test
/eden_export
/eden_export_test
*.o
*.obj

# --- edenadmin operator GUI (admin/, opt-in via build_server.sh --with-admin) ---
/admin/edenadmin

# --- Runtime state written when ./edenserver is run from the repo root ---
/eden_players.txt
/eden_world.model
/eden_signs.txt
/eden_world.edits
/eden_bans.txt
/eden_ops.txt
/edenserver.sock

# --- Logs ---
logs/*.log
logs/*.txt
!logs/.gitkeep

# --- MSVC / Windows build tree (reference project is TCPServer.zip + TCPServer.vcxproj) ---
x64/
TCPServer/
*.pdb
*.ilk
*.idb
*.exe
*.tlog

# --- OS cruft ---
.DS_Store
